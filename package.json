{
  "name": "tea-order-server",
  "version": "1.0.0",
  "type": "module", // 关键：解决 import 语法报错
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.4.3",
    "redis": "^4.6.13",
    "cookie-parser": "^1.4.6",
    "express-useragent": "^1.0.15",
    "vite": "^5.0.10" // 保留原代码中的 vite 依赖（若不用可删除）
  },
  "engines": {
    "node": ">=16.0.0" // 适配 Render 的 Node 版本
  }
}
